# Welcome to lpSolver (Linear Programming Solver)!
lpmodel SampleModel 
{
		
	min 150 * x1 + 150 * x2 + 150 * x3 + 200 * y1 + 200 * y2 
		
	subject to:
		
	x1 + y1 >= 10;
	x1 + x2 + y1 + y2 >= 15;
	x2 + x3 + y1 + y2 >= 10;
	x3 + y2 >= 15;
	
	x1 > 0;
	x2 > 0;
	x3 > 0;
	y1 > 0;
	y2 > 0;
	
};
# __EOF

